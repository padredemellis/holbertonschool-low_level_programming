#include <stdio.h>
#include "main.h"
/**
 *mul - Multiplies two integers.
 * @a: is first integer.
 * @b: is second integer
 *
 * Return: 1 is digit, 0 not digit
 */
int mul(int a, int b)
{
	return (a * b);
}
